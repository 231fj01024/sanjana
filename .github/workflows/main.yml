name: Student Management Workflow

on:
  push:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Cache Python dependencies to speed up future runs
      - name: Cache Python dependencies
        uses: actions/cache@v2
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-python-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-python-

      - name: Set Up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      # Run actual unit tests using pytest
      - name: Run Unit Tests
        run: |
          echo "Running unit tests..."
          pytest --maxfail=1 --disable-warnings -q

      # Run flake8 to lint the code
      - name: Lint Code
        run: |
          echo "Linting code..."
          flake8 .

      # Build package (update this with real build commands)
      - name: Build Package
        run: |
          echo "Building application package..."
          mkdir build
          echo "Build completed successfully!"

      # Simulate Deployment (replace with actual deployment logic)
      - name: Deploy Application
        run: |
          echo "Simulating deployment process..."
          echo "Application deployed successfully (dummy simulation)."

